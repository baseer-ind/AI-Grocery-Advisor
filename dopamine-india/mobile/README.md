# Dopamine India — Mobile (Flutter)

Folder structure placeholder matching `../docs/06-flutter-structure.md`.
Feature modules under `lib/features/categories/*` are not yet implemented
— see `../docs/12-mvp-scope.md` for build order.
